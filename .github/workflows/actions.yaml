name: Names
on:
    push
jobs:
    idle:
        name: Idle job doing only prints
        runs-on: ubuntu-latest
        steps:
            -
                name: checkout
                uses: actions/checkout@v4
            -
                name: step One-Two
                run: |
                    echo Idle Step
                    sleep 20
                    echo Waking up
